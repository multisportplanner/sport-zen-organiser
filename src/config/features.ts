/**
 * Feature flags for MSP.
 *
 * Toggle features on/off without deleting code.
 * Pages always remain accessible via direct URL — flags only control navigation visibility.
 */

export const SHOW_BLOG_IN_NAV = true;
